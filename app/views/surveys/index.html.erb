<header>My Surveys!</header>

<article class="survey">
  <section class="titles">
    <% @surveys.load.each do |survey| %>
        <%= link_to survey.title, survey %>
    <% end %>
  </section>
</article>